# Real-Time-UPI-Fraud-Detection-System_Notebook
A machine learning-based Anomaly Detection system solves this by learning the 'normal' fingerprint of each user's transaction behaviour. It does not rely on hardcoded rules. Instead, it flags any transaction that deviates significantly from that learned normal pattern.
